[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "chatbot-dataset-tools"
version = "0.8.4"
dependencies = [
    "pytest",
    "respx",
    "importlib",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["chatbot_dataset_tools*"]